describe('캐시.spec.ts', () => {
  // function solution(cacheSize: number, cities: string[]): number {}

  it('solution', () => {
    // const result = solution('0001100');
    // console.log(result);
  });
});
