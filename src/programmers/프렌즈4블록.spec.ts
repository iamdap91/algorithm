describe('프렌즈4블록.spec.ts', () => {
  function solution(m: number, n: number, board: string[]) {
    let answer = 0;
    return answer;
  }

  it('solution', () => {
    const result = solution(4, 5, ['CCBDE', 'AAADE', 'AAABF', 'CCBBF']);
    console.log(result);
  });
});
